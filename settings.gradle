/*
// To declare projects as part of a multi-project build use the 'include' method
include 'shared'
include 'api'
include 'services:webservice'
*/

include 'pater-build-base-plugin', 'pater-build-classpath-plugin', 'pater-build-example-plugin'

rootProject.name = 'pater-build'
